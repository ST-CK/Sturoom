{
  "version": 2,
  "builds": [
    { "src": "mcp/main.py", "use": "@vercel/python" },
    { "src": "package.json", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "mcp/main.py" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
